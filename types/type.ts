export type tokenDataTypes = {
  accessToken: string;
  refreshToken: string;
};

export type socialLoginDataType = {
  accessToken: string;
};

export type ourLoginDataType = {
  email?: string;
  password?: string;
};
