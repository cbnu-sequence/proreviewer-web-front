export type tokenDataTypes = {
  accessToken: string;
  refreshToken: string;
};

export type codeLoginDataType = {
  code: string;
};

export type ourLoginDataType = {
  email?: string;
  password?: string;
};
